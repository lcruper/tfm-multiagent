#pragma once

#include <stdbool.h>

/**
 * @brief Take-off and hover routine.
 */
void takeOffRoutine(void);

/**
 * @brief Landing routine.
 */
void landingRoutine(void);

/**
 * @brief Fly in a square pattern routine.
 */
void squareFlightRoutine(void);

/**
 * @brief Rotate around its own axis routine.
 */
void rotateRoutine(void);